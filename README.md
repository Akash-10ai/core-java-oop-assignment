# Student Product Billing & Management System

**Name:** Akash Kumar
**Date:** 2026-04-13

## Concepts Used
- Classes and Objects
- Strings and String Methods (Formatting and Length)
- Arrays and Iteration (Loops)
- Method Overloading (Polymorphism)
- Constructors and Object Initialization
- The `this` keyword and `static` shared variables
- Encapsulation (Getters and Setters)
- Inheritance (Parent and Child classes)

## How to Run
1. Open a terminal or command prompt.
2. Navigate to the `src` folder where the `.java` files are located.
3. Compile the Java code by running: `javac Main.java`
4. Execute the program by running: `java Main`

## Sample Output
Please refer to the `screenshots/output.png` file located in the repository for the exact console output.# core-java-oop-assignment
